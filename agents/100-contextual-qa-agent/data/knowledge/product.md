# Product guide

The sample service exports reports as JSON. An export starts from the Reports
screen and remains available while the browser tab is open. The guide does not
define a server-side retention period.
